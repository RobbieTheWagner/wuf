import barks from 'wuf/utils/barks';
import { module, test } from 'qunit';

module('Unit | Utility | barks', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = barks();
    assert.ok(result);
  });
});
