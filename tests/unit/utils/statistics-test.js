import statistics from 'wuf/utils/statistics';
import { module, test } from 'qunit';

module('Unit | Utility | statistics', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = statistics();
    assert.ok(result);
  });
});
